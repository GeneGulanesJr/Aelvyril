# Phase 11: Web UI — React SPA with Chat, Kanban, Activity Feed, Settings

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the React web UI — chat panel (talks to Supervisor via WebSocket), kanban board view (live ticket status, read-only), agent activity feed (real-time), cost dashboard, and settings page (API keys, model selection, concurrency, watchdog thresholds). No code viewer, no file tree, no editor — full agentic.

**Architecture:** React SPA served by the Orchestrator. Uses WebSocket for real-time updates (ticket transitions, agent activity, cost updates). REST API for settings CRUD. Port select components from the desktop v0.1 archive (CommandPalette, DepGraph, ActivityRow, Workspace, OutputScroll, CSS). Terminal aesthetic with JetBrains Mono, dark theme.

**Tech Stack:** React 18, TypeScript, Vite+, WebSocket, CSS (terminal aesthetic)

**Spec reference:** `docs/superpowers/specs/2026-05-05-cloud-platform-design.md` §3.4, §7.2, §8.1, §9.1

**Depends on:** Phase 1-10

---

## File Structure

```
ui/                             # Separate from server code
├── package.json
├── vite.config.ts
├── index.html
├── src/
│   ├── App.tsx                 # Main layout (chat + kanban + activity)
│   ├── main.tsx                # Entry point
│   ├── hooks/
│   │   └── useWebSocket.ts     # WebSocket connection hook
│   ├── components/
│   │   ├── ChatPanel.tsx       # Chat input/output with Supervisor
│   │   ├── KanbanBoard.tsx     # 5-column kanban view (read-only)
│   │   ├── TicketCard.tsx      # Single ticket card
│   │   ├── ActivityFeed.tsx    # Real-time agent activity
│   │   ├── CostDashboard.tsx   # Token/cost per agent/ticket/session
│   │   ├── SettingsPage.tsx    # API keys, models, concurrency, watchdog
│   │   ├── DepGraph.tsx        # Ticket dependency graph (ported from v0.1)
│   │   └── CommandPalette.tsx  # Fuzzy search (ported from v0.1)
│   ├── api/
│   │   └── client.ts           # REST API client
│   └── styles/
│       └── terminal.css        # Terminal aesthetic (ported from v0.1)
tests/
└── components/
    ├── ChatPanel.test.tsx
    ├── KanbanBoard.test.tsx
    └── SettingsPage.test.tsx
```

### Task 1: Scaffold React app with Vite+
- [ ] Create ui/ directory, init with vp/Vite, install React + testing deps → commit

### Task 2: WebSocket hook
- [ ] Write failing test → implement useWebSocket hook → pass → commit

### Task 3: Chat panel component
- [ ] Write failing test → implement ChatPanel with message input/output → pass → commit

### Task 4: Kanban board component
- [ ] Write failing test → implement 5-column board with TicketCards → pass → commit

### Task 5: Activity feed component
- [ ] Write failing test → implement real-time agent activity feed → pass → commit

### Task 6: Cost dashboard component
- [ ] Write failing test → implement cost display per agent/ticket → pass → commit

### Task 7: Settings page
- [ ] Write failing test → implement API key input, model selection, concurrency slider → pass → commit

### Task 8: Port terminal CSS
- [ ] Copy CSS from archive/desktop, adapt for web (drop TitleBar styles) → commit

### Task 9: Wire up App.tsx with layout
- [ ] Compose all components in split-pane layout → commit

### Task 10: Run all Phase 11 tests
- [ ] Run: `npx vitest run` → ALL PASS → commit
